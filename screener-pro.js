/**
 * Screener Pro Dashboard Manager
 */

const ScreenerPro = {
    init() {
        // Los widgets de TradingView se cargan solos
    }
};

document.addEventListener('DOMContentLoaded', () => ScreenerPro.init());
